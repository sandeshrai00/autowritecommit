{
  "name": "auto-commit-message",
  "displayName": "Auto Commit Message",
  "description": "Deep analyze git changes and generate detailed conventional commit messages",
  "version": "1.2.0",
  "publisher": "autocommit",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "keywords": [
    "git",
    "commit",
    "message",
    "conventional-commits",
    "auto-generate"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "auto-commit-message.generateMessage",
        "title": "Generate Commit Message",
        "category": "Auto Commit",
        "icon": "$(sparkle)"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "auto-commit-message.generateMessage",
          "when": "scmProvider == git",
          "group": "navigation@1"
        }
      ],
      "scm/sourceControl": [
        {
          "command": "auto-commit-message.generateMessage",
          "when": "scmProvider == git",
          "group": "inline@1"
        }
      ]
    },
    "configuration": {
      "title": "Auto Commit Message",
      "properties": {
        "autoCommitMessage.useEmoji": {
          "type": "boolean",
          "default": false,
          "description": "Include emoji in commit messages"
        },
        "autoCommitMessage.maxLength": {
          "type": "number",
          "default": 72,
          "description": "Maximum length of the commit message"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "1.85.0",
    "@types/node": "20.10.0",
    "typescript": "^5.3.2",
    "@vscode/vsce": "^2.22.0"
  }
}